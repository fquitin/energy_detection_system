Each folder contains the source file of each fpga image. 
Each module has its folder and its makefile that can be used to synthesized the custom fpga image.
Each folder can be copied on the FPGA source code in the custom folder.
The "custom_dsp_rx.v" has to be modified inserting the module.
